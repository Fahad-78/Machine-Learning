# Take a full name from the user (e.g. "fahad hossain"). Capitalize it properly and print each word on a separate line.

name = str(input("Enter your fullname in lowercase: "))

name1 = name.capitalize()

for i in name1:
    print(i)