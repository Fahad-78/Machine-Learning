#Keywords (We can't use this as variables name)

'''
False       def         if          rais
None        del         import      return
True        elif        in          try
and         else        is          while
as          except      lambda      with
assert      finally     nonlocal    yield
break       for         not
class       from        or
continue    global      pass
'''

#Identifiers
'''
->We can't start with a digit
->we can use special character
->Identifiers can not be akeyword
'''